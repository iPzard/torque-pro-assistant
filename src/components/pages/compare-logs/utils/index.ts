/**
 * Barrel for utils consumed only by the Compare page. Grows with the
 * summary table delta calcs, alignment options, etc.
 */
export type { Alignment, OverlayDataset, OverlayRow, OverlaySeries } from './build-overlay-dataset';
export { buildOverlayDataset } from './build-overlay-dataset';
